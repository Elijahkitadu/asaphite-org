import { storySchema       } from './story'
import { campaignSchema    } from './campaign'
import { blogPostSchema    } from './blogPost'
import { impactStatSchema, teamMemberSchema, churchPartnerSchema } from './other'

export const schemaTypes = [
  storySchema,
  campaignSchema,
  blogPostSchema,
  impactStatSchema,
  teamMemberSchema,
  churchPartnerSchema,
]
